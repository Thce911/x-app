export class Course {
    constructor(
        public courseName: string,
        public professor: string,
        public email: string
    ) { }
}

export class Pendejada {
    constructor(
        public elements
    ) {    }
}
